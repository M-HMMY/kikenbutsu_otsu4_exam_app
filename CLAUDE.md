# CLAUDE.md

このファイルは、このリポジトリで作業するときに最初に読む前提です。

## このアプリは何か

**危険物取扱者試験 乙種第 4 類**（一般財団法人消防試験研究センターが実施）の学習アプリ。**教本を軸にした学習アプリ**であって、問題集アプリではありません。

**コードは `C:\Dev\genai_passport_exam_app` から持ってきてあります。**`fe_exam_app` から数えて **6 つめ**の姉妹アプリで、画面・記法・運用の作法は共通です。迷ったら向こうの実装を見れば答えがあります。

**まず [docs/handover.md](docs/handover.md) を読んでください。**系譜 5 本ぶんの作り方・繰り返し出る誤りの型・Codex の回し方がまとまっています。このファイルは「このアプリをどう作るか」、あちらは「**学習アプリ全般をどう立ち上げるか**」です。

## 姉妹アプリと決定的に違う 4 点（最初に読む）

**ここを読み飛ばすと、姉妹アプリの前提をそのまま持ち込んで全部やり直しになります。**

### 1. 五肢択一（選択肢が 5 つ）

姉妹アプリは全部**四肢択一**でした。この試験は**五肢択一式**です。

- `Question.choices` は **5 要素のタプル**
- `answer` は **0〜4**
- `check.ts` の検査、`choiceIndexOf` の既定値、正解の位置の偏りの閾値（**ならせば 20 %。25 % ではない**）も 5 に合わせてあります

**姉妹アプリから問題を写すときは必ず引っかかります。**`npm run check` が「選択肢が 4 個」でエラーにします。

### 2. 合格基準が公表されていて、科目ごとに足切りがある

**これが最大の違いです。**姉妹アプリ（生成AIパスポート・G 検定）は合格基準が**非公表**だったので、「アプリが勝手に合格と断定しない」という方針を守っていました。

**この試験は違います。**「試験科目ごとの成績が、それぞれ **60 % 以上**」と公表されています。しかも**合計点ではありません。**

| 科目 | 略称 | 問題数 | 必要な正解数 |
| --- | --- | --- | --- |
| 危険物に関する法令 | 法令 | 15 問 | **9 問** |
| 基礎的な物理学及び基礎的な化学 | 物化 | 10 問 | **6 問** |
| 危険物の性質並びにその火災予防及び消火の方法 | 性消 | 10 問 | **6 問** |

だから **`src/pages/Mock.tsx` は科目ごとに判定を出します。**`PASS_RATIO`（`src/data/categories.ts`）が基準値です。

**ただし出してよいのは「この模試の結果が基準を満たすか」まで。**収録した問題は本番ではないので、**本番の合否を予想する形にはしないでください。**ここは姉妹アプリの方針をそのまま引き継ぎます。

**「法令を捨てて物化で稼ぐ」は成り立ちません。**教本でも問題でも、読者がそう誤解する書き方をしないこと。

### 3. 計算問題が出る

「基礎的な物理学及び基礎的な化学」には計算が出ます。比熱と熱量、熱膨張による体積変化、密度、濃度、気体の体積あたりです。

**姉妹アプリでは `src/data/drills.ts` が骨格だけの空ファイルでした**（あちらは計算問題が出ない試験だったため）。**このアプリでは中身を作ってください。**値を振り直して出題する仕組みは既に入っています。

### 4. 公表されたシラバスが無い

姉妹アプリは、公式シラバスの詳細キーワード（約 200 語）を全部書き起こして、それを後工程すべての土台にしていました。

**この試験にはそれがありません。**公表されているのは**科目名と問題数だけ**です。

そのため出題範囲は、**消防法・危険物の規制に関する政令・同規則**と、消防試験研究センターが公開している**過去に出題された問題**から起こすしかありません。**ここが最初の大仕事です。**（`docs/` に、姉妹アプリの `syllabus-*.md` に相当するものを作ってください。）

## 試験の形

**2026 年 9 月 13 日に [消防試験研究センター](https://www.shoubo-shiken.or.jp/kikenbutsu/) で確認した内容です。**要項は改訂されるので、作業を始める前にもう一度見てください。

| 項目 | 内容 |
| --- | --- |
| 形式 | **五肢択一式**のマークカード方式。記述なし |
| 出題数 | **35 問**（法令 15 / 物化 10 / 性消 10） |
| 試験時間 | **2 時間**（120 分）。1 問あたり約 3 分 26 秒 |
| 合格基準 | **試験科目ごとに 60 % 以上**（公表） |
| 受験資格 | **なし** |
| 受験手数料 | **乙種 5,300 円** |
| 実施 | **都道府県ごと。**全国一斉ではなく、回数も日程も県によって違う |
| 科目免除 | すでに乙種免状を 1 つ持っていれば、他の類では**法令と物化が免除**（解答 10 問 / 35 分）。**模試に「科目免除」の形式として入れてあります** |
| 合格率 | **31.9 %**（令和 7 年度。受験者 222,545 人 / 合格者 71,059 人）。**乙 4 だけで危険物試験全体の 71 %** |

**時間には余裕があります。**姉妹アプリ（1 問 60 秒）と違い、読み切るのが精一杯ということはありません。そのぶん**問題文の長さの上限も緩めてあります**（`check.ts` は 200 字で注意・300 字でエラー）。

## 読者を誰に置くか（**決めました**）

> **一文で言うと：中学理科は習ったが、高校で化学を取らなかった社会人。**
> 仕事で必要になって受けることになり、化学の教科書は 10 年以上開いていない。

**2026 年 9 月 13 日に決定。**根拠は次の 3 つです。

- **受験資格がありません。**甲種と違い、化学系の学歴要件がない
- **受験者が桁違いに多い。**令和 7 年度で **222,545 人**（危険物試験全体 311,230 人の 71 %）。
  母集団が広いぶん、予備知識の下限に合わせるしかない
- **合格率 31.9 %。**3 人に 1 人しか受かりません。落ちている 15 万人を拾う教本にすること

（数字の出典：消防試験研究センター「試験実施状況」令和 7 年度。2026 年 9 月 13 日確認）

### 通じる語・通じない語

**「通じない」に挙げた語は、その節で定義してから使うこと。**定義なしに出さないでください。

| | 例 |
| --- | --- |
| **通じる**（中学理科・日常語） | 固体・液体・気体、状態変化、融点、沸点、蒸発、密度、「水より軽い」、温度、熱、酸素、二酸化炭素、水素、窒素、燃える、原子、分子、簡単な化学反応式、酸性・アルカリ性、静電気、電気を通す／通さない |
| **通じない**（高校化学以降） | **モル（物質量）**、**比熱**、熱容量、潜熱・顕熱、**官能基**、示性式、構造式、**酸化剤・還元剤**、電子の授受による酸化還元の定義、**ヘスの法則**、生成熱・燃焼熱、飽和蒸気圧、**蒸気比重**、体積膨張率、**燃焼範囲（vol %）**、有機化合物・無機化合物、触媒・負触媒、昇華 |
| **試験固有語**（日常語ではない。必ず定義する） | **引火点・発火点・燃焼点**、指定数量、製造所等、保安距離、保有空地、危険等級、所要単位・能力単位 |

**「酸化」は半分だけ通じます。**中学理科では「酸素と結びつくこと」まで習っています。
**「水素を失う」「電子を失う」は通じません。**中学の定義から接ぎ木してください。

**「比重」も半分です。**「水より重い・軽い」は通じますが、**「蒸気比重」は通じません**
（空気を 1 とすることを知らない）。

### 書き方への影響

- **物化の章を厚くする。**モル・比熱・官能基をその場で立ち上げるぶん、
  法令や性消より 1 節あたりの分量が増えます。章立てでそのぶんを見込んであります
- **公開問題 問 22（ヘスの法則で生成熱）のような問題は、
  公式を出す前に「なぜ足し引きでいいのか」から書く。**公式だけ渡すと解けません
- **法令は予備知識が要りません。**ここは語彙ではなく**量**との戦いなので、
  表と ` ```quiz ` を厚くして、読ませるより引かせる作りにしてください

## 変えないこと

- **ランタイム依存は React だけ。**ルータ・Markdown・図はすべて自前。ライブラリを足す前に、本当に必要か検討する
- **外部通信をしない。**学習記録は localStorage のみ。サーバもアカウントもない
- **オフラインで動く。**PWA として成立させる
- TypeScript は strict（未使用変数もエラー）
- **合否は「この模試の結果が基準を満たすか」までしか出さない。**本番の合否予想はしない

## 節の書き方

本文は Markdown サブセットです。1 つの節は次の骨格で書きます。

```
# ざっくり言うと          ← 日常語で「何の話か」
（本論。見出し・表・図）
> **試験のポイント** …     ← 暗記すべき事項
> **よくある勘違い** …     ← 先回りして潰す誤解
# この節のまとめ           ← 箇条書き 5〜6 個
```quiz                    ← 一問一答（採点も記録もしない）
問い :: 答え
```
```

「この節のまとめ」「試験のポイント」「よくある勘違い」の 3 つは、`src/lib/digest.ts` が自動で抜き出して**直前チェックシート**に並べます。**書式を崩すと拾われません。**

**数字を問う試験なので、` ```quiz ` を厚めに入れてください。**指定数量・引火点・発火点・保有空地の幅は一問一答向きです。

### 使える記法

姉妹アプリと同じです。`compare` 図は「1 行 1 セル、偶数行が左・奇数行が右」で書きます（要素が奇数だと `npm run check` がエラーにします）。図の中では `**強調**` が効きません。

**数式（`$...$`）は使えます**が、この試験の計算は四則演算が中心なので、使う場面はほとんどないはずです。`src/lib/math.tsx` は骨格として残してあります。

### バックスラッシュの落とし穴（重要）

本文は TypeScript の**テンプレートリテラル**の中にあります。

| 書きたいもの | ファイルに書く形 |
| --- | --- |
| `\n` を文字として出す | `\\n` |
| バックティック | バックスラッシュ + バックティック |
| `${` | `\${` |

さらに、**bash のヒアドキュメント（`cat > file <<'EOF'`）はバックスラッシュを勝手に落とします。**姉妹アプリで何度も壊しました。Write ツールを使うか、`scripts/emit_textbook.py` を使ってください。

## 問題の書き方

- **選択肢は 5 つ。**四肢択一ではありません
- **`sectionId` は必ず付ける。**解説から教本へ戻る導線と、節ごとの出題に使う
- **正解の位置は散らす。**五肢択一なので、ならせば各 20 %
- 解説では、正解の理由だけでなく**誤答がなぜ誤りか**にも触れる
- **誤答も正解と同じ密度で書く。**正解だけが長いと、読まずに「長いものを選ぶ」で当てられます
- **「必ず」「すべて」を誤答専用にしない。**言い切りが誤答側にしか出てこないと、それ自体が手掛かりになります
- **数字を問う問題では、一次資料の値を確かめてから書く**

上の後半 3 つは `npm run check` が数えて注意を出します。

## 作業の作法

### 必ず通すもの

```
npm run check   # データの整合性 + 実際に描いてみる検査
npx tsc --noEmit
npm run build
```

`npm run check` は「型では防げない食い違い」を捕まえます。ID 重複、参照切れ、記法の崩れ、図の書式、リンクの飛び先、ドリルの選択肢、節をまたいだ問題の重複、そして**実際に React で描画して NaN や崩れが出ないか**まで見ます。**新しい不整合の型を見つけたら、直すついでに `scripts/check.ts` へ検査を足してください。**

### 内容の正しさ

**書いた本人が読み返しても間違いは見つかりません。**系譜 6 回すべてで、自己レビューの発見数は **0 件**でした。実績は [docs/handover.md](docs/handover.md) の §4 にあります。

まとまった量を書いたら、**書いた人とは別の目で検証する工程を必ず入れてください。**省略できる工程ではありません。

**このアプリに固有の検証観点が 3 つあります。**

1. **数字が一次資料と合っているか。**指定数量、引火点、発火点、燃焼範囲、保有空地、タンク容量。**この試験は数字そのものを問います。**書いた人以外が消防法・政令・規則で確かめること
2. **法令が現行か。**消防法と関係政令・規則は改正されます。**「確認済み」と書いた事実にも期限があります**（姉妹アプリで、確認して書いた施行日が**施行の 6 日前に延期**されていた例があります）
3. **科目の線引きが合っているか。**燃焼・消火の理論は「物化」と「性消」のどちらでも扱われます。**同じ用語を章によって違う説明にしない**こと

### 系譜で実際に出た誤りの型

**同じ型は繰り返し起きます。**[docs/handover.md](docs/handover.md) の §5 に全部並べてあるので、書くときも直すときも手元に置いてください。とくに多いのは次の 4 つです。

- **誤答の説明もれ**（最も多い）
- **誤答が実は正しい**（最も重い）
- **正解だけが突出して長い**
- **言い切りが誤答だけに偏る**

### 公開のルール

公開リポジトリにする前提です。

- **コミットの署名は GitHub の noreply アドレスを使う。**実メールアドレスはコミット履歴に永久に残ります
- 個人情報・ローカルパス・API キーを含めない
- **`.github/workflows/deploy.yml` の対象ブランチを、実際のブランチ名に合わせること。**このリポジトリは `main` に合わせてあります

## いまの状態

**土台が固まりました。教本の本文はこれからです。**

| 項目 | 状況 |
| --- | --- |
| 骨格の移植 | **済**（2026 年 9 月 13 日）。`npm run check` / `npx tsc --noEmit` / `npm run build` の 3 つが通る |
| 試験要項の確認 | **済**（2026 年 9 月 13 日）。35 問 / 120 分 / 五肢択一 / 科目ごと 60 %。公式の「試験の方法」「試験科目及び問題数」で全項目を再確認した |
| 五肢択一への対応 | **済**。型・検査・キーボード操作・位置の偏りの閾値を 5 に合わせた |
| 科目ごとの合否判定 | **済**。`PASS_RATIO` と `Mock.tsx` の科目別判定 |
| **科目免除** | **済**（2026 年 9 月 15 日）。模試に「科目免除」の形式（**性消 10 問 / 35 分**）を足した。判定表では免除された科目を**「出題なし」ではなく「免除」**と書き分ける |
| **出題範囲の洗い出し** | **済**（2026 年 9 月 13 日）。[docs/syllabus.md](docs/syllabus.md)。一次資料と公開問題 35 問から自前で起こした |
| **数値の一次資料台帳** | **済**。[docs/primary-numbers.md](docs/primary-numbers.md)。A 節＝法令の値、**B 節＝物性値も 24 物質ぶん埋めた**（消防庁「危険物災害等情報支援システム」）。**残りは B 節末尾の一覧のとおり** |
| **公開問題の分析** | **済**。[docs/public-questions.md](docs/public-questions.md)。35 問が何を問うたか |
| **読者** | **決定**（2026 年 9 月 13 日）。中学理科までの社会人。上の「読者を誰に置くか」を参照 |
| **章立て・節割り** | **確定**。10 章 43 節。[docs/section-plan.md](docs/section-plan.md) と `CATEGORIES` |
| 入門編 | **済**（2026 年 9 月 13 日）。**4 節すべて手で書いた。**`i-1` 試験の形 / `i-2` 歩き方 / `i-3` 計算の解き方 / `i-4` 用語ミニ辞典。**これが文体の見本** |
| 教本の本文 | **43 節すべて書いた**（2026 年 9 月 13 日）。**レビュー反映後 103,693 字**（2026 年 9 月 14 日）。入門編 4 節は手書き、残り 39 節は Codex に書かせた。**別の目によるレビュー済**（Codex 6 ジョブ・**130 件**。一次資料で裏を取って反映） |
| 確認問題 | **105 問**（法令 45 / 物化 30 / 性消 30）。**教本とは別の側が書いた。**設問の形は誤り選択 62 % で、本番（約 63 %）に寄せてある（**科目ごとに割ると 法令 47 % / 物化 67 % / 性消 80 %**。下の「決めていないこと」を参照）。**別の目によるレビュー済**（Codex 3 ジョブ・**51 件**。うち「正解が定まらない」**13 件**） |
| 計算ドリル | **6 種類**。比熱と熱量 / 熱膨張 / 比重から質量 / 質量パーセント濃度 / ヘスの法則 / 指定数量の倍数。**五肢択一に直してある**（姉妹アプリは 4 択だった） |
| 体験ウィジェット | **3 個。**`baisu`（指定数量の倍数）/ `inkaten`（引火点・燃焼点・発火点）/ `shouka`（燃焼の 3 要素を外す）。それぞれ `lw-4` / `sf-2` / `ss-1` に埋め込んである |
| 執筆プロンプト | **13 本すべて済**（2026 年 9 月 13 日）。共通 4 本＋章ごと 9 本。走らせる順番は [scripts/prompts/README.md](scripts/prompts/README.md) |
| アイコン | **済**。`scripts/make_icons.py`。**朱 → 橙に白い「4」**（姉妹アプリと色がぶつからないように選んである） |
| ランチャ | **済**。`scripts/launch.cmd` → `launch.ps1`、`scripts/make_shortcut.ps1` |
| git | **ローカルのみ。**まだ公開していない。**公開前に「やり残し」を片づけること** |

## 次にやること

**順番に意味があります。**引き継ぎ書の §2 の順です。
**1〜11 は終わりました。残りは下の「やり残し」です。**

1. ~~出題範囲を洗い出す~~ … **済。**[docs/syllabus.md](docs/syllabus.md)
2. ~~読者を決める~~ … **済。**中学理科までの社会人
3. ~~章立てと節割りを確定する~~ … **済。**10 章 43 節
4. ~~入門編を手で書く~~ … **済。**4 節。**`i-1`〜`i-4` が文体の見本です。**
   章のプロンプトに「これを読んで文体を合わせろ」と渡してください
5. ~~物性値を確かめて B 節を埋める~~ … **済。**24 物質。
   **ただし「まだ確かめていないもの」が 6 項目残っています**
   （灯油の 40 ℃、重油、軽油、二硫化炭素の燃焼範囲、ガソリンの燃焼範囲、ヨウ素価の境目）。
   **表の † は「消防庁のデータベースに項目が無い」印で、埋めてよい空欄ではありません**
6. ~~共通のプロンプトを書き換える~~ … **済。**4 本とも。
   読者像・43 節の ID 一覧・名指しの釘・数値台帳の使い方を入れてある
7. ~~章ごとの執筆プロンプトを 9 本書く~~ … **済。**`02-law-what.md` 〜 `10-prop-each.md`
8. ~~教本を書かせる → 問題を書く~~ … **済。**43 節 98,934 字 / 105 問。
   **ただしレビューがまだです。**下の「やり残し」へ
9. ~~計算ドリルを作る~~ … **済。**6 種類
10. ~~体験ウィジェットを作る~~ … **済。**3 個
11. ~~ブラウザで動かす~~ … **済。**`node scripts/drive.mjs`。
    43 節・ドリル・確認問題・ウィジェットまで押して、コンソールエラーなし
12. ~~別の目でレビューする~~ … **済**（2026 年 9 月 14 日）。Codex 9 ジョブ（教本 6 / 確認問題 3）で **181 件**。
    **自己レビューの発見数は、系譜 7 回目もやはり 0 件でした。**
13. **公開する** … **下のやり残しを片づけてから**

---

## やり残し（公開の前に）

**1. ~~教本と確認問題のレビュー（別の目）~~ … 済**（2026 年 9 月 14 日）。

Codex に 9 ジョブ走らせ、**181 件**の指摘を一次資料で裏を取って反映しました。
プロンプトは残してあるので、**本文を書き足したら同じものを走らせてください。**

| 走らせるもの | 中身 |
| --- | --- |
| `scripts/prompts/review/r1-intro-lw.md` 〜 `r6-pe.md` | 教本 43 節を 6 本に分けたもの |
| `scripts/prompts/review/q1-law.md` 〜 `q3-prop.md` | 確認問題 105 問を**科目の境目で** 3 本に分けたもの |
| `scripts/prompts/91-review-questions.md` | 上の q1〜q3 のひな形 |

**とくに重かった指摘（いずれも直しました）：**

- **「誤答が実は正しい」が 13 件。**仮貯蔵・仮使用の例外、指定施設の限定、
  乙種は「取り扱う類」に限ること、水の 0〜4 ℃の体積、水に溶ける液体は浮かないこと
- **「〜のものがある」を類の全体へ広げていた**のが 3 件。
  教本は正しく限定しているのに、問題の選択肢で限定が落ちていた
- **教本に書いていないことを問題だけが問うていた**のが 2 件
- **同じ適応表を 4 問で焼き直していた。**しかも 1 問は科目の線引きを越えていた

```bash
codex exec --cd "C:/Dev/kikenbutsu_otsu4_exam_app" --sandbox read-only --color never \
  - < scripts/prompts/review/r1-intro-lw.md > review-r1.txt 2>&1
```

> **`--sandbox read-only` のときは `-o` が使えません。**
> 出力ファイルの書き込みもサンドボックスに止められます。
> **標準出力をリダイレクトしてください。**

**2. 公開問題 35 問は収録できません。**
消防試験研究センターの「[このサイトについて](https://www.shoubo-shiken.or.jp/d/)」に
**「当ウェブサイトのコンテンツを許可なく複製、転用、販売など二次利用することを禁じます」**
と明記されています。**収録するなら先に許可を取ってください**
（[docs/public-questions.md](docs/public-questions.md) に経緯を書きました）。

**3. B 節の未確認 6 項目**は、まだ未確認のままです。
教本では値を書かずに回避してあるので、公開の障害にはなりません。

### 決まったこと（2026 年 9 月 15 日）

- **科目免除をアプリで表現しました。**模試に 4 つめの形式として入れてあります。
  実装で効いたのは、**問題数を減らすことではなく判定の見せ方**でした。
  - 出題を**性消だけ**に絞る（不足分を他章から補うときも、科目の外へ出ない）
  - 収録数が足りるかの判定も、**絞った科目の中**で見る。
    全体の問題数で見ると、性消が 10 問未満でもボタンが押せてしまう
  - 判定表では、免除された科目を**「出題なし」ではなく「免除」**と書く。
    **空欄に見えると、点が取れなかったのかと読める**
  - 免除では**足切りが 1 つしかない**ので、「ほかの科目で取り返せない」と明示する。
    通常形式より厳しい面がある、という事実のほうが伝えるべきこと
  - 設定画面に「**免除は乙種どうしの話で、甲種には及ばない**」と書く。
    乙 4 が最初の 1 枚になる人が押して、10 問で足りると誤解しないように

  なお `scripts/drive.mjs` に、**この形式を 10 問とも解いて判定表まで見る筋書き**を足しました。
  最初に書いた筋書きはボタン名（「次の問題へ」／実際は「次の問題 →」）と
  セレクタを外していて、**アプリではなく筋書きのほうが押せていませんでした**
  （引き継ぎ書 §7 の型そのもの）。

### 決まったこと（2026 年 9 月 14 日）

- **設問形式の偏りは、直しません。**レビューが
  「法令の誤り選択 44 % は、本番の 63 % よりかなり低い」と指摘してきましたが、
  **63 % は本番『全体』の数字で、科目ごとの数字ではありませんでした。**
  公開問題 35 問を科目ごとに数え直すと **法令 33 % / 物化 50 % / 性消 100 %** で、
  **本番のほうが偏っています。**しかも偏りの向きが同じ（法令がいちばん
  「正しいものを選ぶ」寄り、性消がいちばん「誤っているものを選ぶ」寄り）。
  うちは 法令 47 % / 物化 67 % / 性消 80 % なので、**本番より穏やかな側**です。
  `scripts/check.ts` の検査は「本番へ寄せる」ためのものをやめ、
  **その科目が一方の形だけになっていないか**だけを見るようにしました
  （40〜85 % という幅は、**本番の法令と性消をどちらも弾いてしまいます**）

- **`pc-5`（事故事例）は残します。**科目名が
  「危険物の性質**並びにその火災予防及び消火の方法**」である以上、
  事故の場面へ当てはめる節は科目の中心にあります。
  **公開問題 35 問に出ていないのは 1 回分のサンプルにないというだけ**で、
  範囲外の根拠にはなりません（[docs/syllabus.md](docs/syllabus.md) 3-5 に理由を書きました）。
  あわせてレビューの指摘 3 件を直しました——
  「新しい知識を増やさない」という事実と違う宣言、
  混触の原因を第 4 類側の性質に割り当てていた誤り、
  確認問題 3 問が**教本に書いていないこと**（濃度測定・導電性の装備・土のう・吸着材）を
  問うていた件です

### 決まったこと・分かったこと（2026 年 9 月 13 日）

- **合格率は 31.9 %**（令和 7 年度）。受験者 **222,545 人**、合格者 71,059 人。
  危険物試験全体 311,230 人の **71 % が乙 4** です。
  出典は公式の[試験実施状況](https://www.shoubo-shiken.or.jp/result/)。
  **403 ではなく、`/result/` の年度別ページから読めます**
- **公開問題の PDF は読めます。**AES 暗号化されていますが、
  `pip install cryptography` を入れれば `pypdf` で開きます（パスワードは不要）。
  35 問と解答を全部抽出して [docs/public-questions.md](docs/public-questions.md) に分析を置きました
- **公開問題 35 問は原文のまま収録する方針**に決めました。`Question.source` を使うこと。
  **公開前に、出典表示の作法を消防試験研究センターの利用条件で確かめてください**

- **物性値の出典は消防庁の「危険物災害等情報支援システム」です。**
  `https://internal.fdma.go.jp/kiken-info/list/list.html` が全物質の一覧で、
  そこから CAS 番号で詳細ページ（`material/m_<番号>.html`）を引けます。
  **引火点・発火点・比重・蒸気比重・爆発範囲・色・臭気・水溶性**という、
  **この試験が問う項目そのもの**が載っています
- **出典によって値が食い違います。**アセトンの引火点は消防庁 −18 ℃、厚労省 SDS −20 ℃。
  灯油に至っては 35 ℃ / 37〜65 ℃ / 40 ℃（教材）の 3 通り。
  **教本では値を決め打ちせず、「−20 ℃前後」「常温より低い」という向きで書いてください**

### まだ決めていないこと

**ありません**（2026 年 9 月 15 日に科目免除を入れて、最後の 1 件が片づきました）。

**残っているのは「まだ確かめていないこと」だけです。**
下の「やり残し」の 3（B 節の未確認 6 項目）と、
B 節の ‡ 印を付けた 3 項目（キシレン・二硫化炭素の蒸気比重、アクリル酸の沸点と比重）。
**いずれも教本では値を書かずに回避してあるので、公開の障害にはなりません。**
